import React,{useState} from 'react';import axios from 'axios';const ComplianceUI=()=>{const[activeTab,setActiveTab]=useState('docs');const[docs,setDocs]=useState([]);const headers={Authorization:`Bearer ${localStorage.getItem('token')}`};const createDoc=async()=>{try{const res=await axios.post('/api/compliance/docs',{},{headers});setDocs([...docs,res.data]);}catch(e){console.log(e);}};return(<div><h1>Compliance UI</h1><button onClick={createDoc}>Create Doc</button><table><tr><th>ID</th><th>Type</th></tr>{docs.map(d=>(<tr key={d.id}><td>{d.id}</td><td>{d.type}</td></tr>))}</table></div>);};export default ComplianceUI;
